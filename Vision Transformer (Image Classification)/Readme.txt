The code contains two different architectures, namely CNN and Transformer.
Both architectures have 3 different variants, base, small and tiny.
Each of the architecture can be trained and evaluated using benchmark datasets.
Some of the experimentations are performed in the provided jupyter notebook.